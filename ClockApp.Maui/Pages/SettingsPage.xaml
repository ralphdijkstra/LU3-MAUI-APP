<?xml version="1.0" encoding="utf-8" ?>
<pages:MvvmContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:ClockApp.Maui.Controls"
             xmlns:pages="clr-namespace:ClockApp.Maui.Pages"
             xmlns:vm="clr-namespace:ClockApp.Maui.ViewModels"
             x:Class="ClockApp.Maui.Pages.SettingsPage"
             x:TypeArguments="vm:SettingsViewModel"
             x:DataType="vm:SettingsViewModel"
             Style="{StaticResource TimeOnPage}">

    <Grid RowDefinitions="Auto,*">
        <VerticalStackLayout Grid.Row="0" Spacing="0">
            <Border
                BackgroundColor="{StaticResource Surface}"
                StrokeThickness="0"
                HeightRequest="52"
                Padding="16,0,12,0">
                <controls:ClockTitleView BindingContext="{Binding}" VerticalOptions="Center" />
            </Border>

            <BoxView
                HeightRequest="1"
                Color="{StaticResource Border}"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>

        <ScrollView Grid.Row="1">
            <VerticalStackLayout Padding="20" Spacing="20">
                <Label Text="Instellingen" Style="{StaticResource TimeOnTitle}" FontSize="22" />
                <Border Style="{StaticResource TimeOnCard}" Padding="24,28">
                    <VerticalStackLayout Spacing="12">

                        <Label Text="Account" Style="{StaticResource TimeOnSectionTitle}" Margin="0" />

                        <VerticalStackLayout Spacing="8">
                            <Border Stroke="{StaticResource Border}" StrokeThickness="1" StrokeShape="RoundRectangle 12" Padding="16,14">
                                <VerticalStackLayout Spacing="10">
                                    <VerticalStackLayout Spacing="2">
                                        <Label Text="Naam" Style="{StaticResource TimeOnSubtitle}" />
                                        <Label Text="{Binding DisplayName}" Style="{StaticResource TimeOnSectionTitle}" FontAttributes="None" Margin="0" />
                                    </VerticalStackLayout>

                                    <VerticalStackLayout Spacing="2">
                                        <Label Text="E-mailadres" Style="{StaticResource TimeOnSubtitle}" />
                                        <Label Text="{Binding Email}" Style="{StaticResource TimeOnSectionTitle}" FontAttributes="None" Margin="0" />
                                    </VerticalStackLayout>
                                </VerticalStackLayout>
                            </Border>

                            <Border Stroke="{StaticResource Border}" StrokeThickness="1" StrokeShape="RoundRectangle 12" Padding="16,14">
                                <VerticalStackLayout Spacing="2">
                                    <Label Text="Organisatie-ID" Style="{StaticResource TimeOnSubtitle}" />
                                    <Label Text="{Binding OrganisationId}" Style="{StaticResource TimeOnSectionTitle}" FontAttributes="None" Margin="0" />
                                </VerticalStackLayout>
                            </Border>
                        </VerticalStackLayout>

                        <VerticalStackLayout IsVisible="{Binding IsManager}" Spacing="12" Margin="0,16,0,0">
                            <Label Text="Inklokmethode" Style="{StaticResource TimeOnSectionTitle}" Margin="0" />

                            <Label Text="Kies hoe medewerkers in de app inklokken." Style="{StaticResource TimeOnSubtitle}" />

                            <VerticalStackLayout Spacing="8">
                                <Border Stroke="{StaticResource Border}" StrokeThickness="1" StrokeShape="RoundRectangle 12" Padding="16,14">
                                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="4">
                                        <RadioButton Grid.Column="0" GroupName="CheckInMode" IsChecked="{Binding IsSelfModeSelected}" InputTransparent="True" VerticalOptions="Center" />
                                        <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                                            <Label Text="Zelf inklokken" Style="{StaticResource TimeOnSectionTitle}" FontAttributes="None" Margin="0" />
                                            <Label Text="Medewerker klokt zelf in via de knop." Style="{StaticResource TimeOnSubtitle}" />
                                        </VerticalStackLayout>
                                        <Grid.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding SelectSelfModeCommand}" />
                                        </Grid.GestureRecognizers>
                                    </Grid>
                                </Border>

                                <Border Stroke="{StaticResource Border}" StrokeThickness="1" StrokeShape="RoundRectangle 12" Padding="16,14">
                                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="4">
                                        <RadioButton Grid.Column="0" GroupName="CheckInMode" IsChecked="{Binding IsQrModeSelected}" InputTransparent="True" VerticalOptions="Center" />
                                        <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                                            <Label Text="QR-code" Style="{StaticResource TimeOnSectionTitle}" FontAttributes="None" Margin="0" />
                                            <Label Text="Medewerker scant een QR-code of vult een koppelcode in." Style="{StaticResource TimeOnSubtitle}" />
                                        </VerticalStackLayout>
                                        <Grid.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding SelectQrModeCommand}" />
                                        </Grid.GestureRecognizers>
                                    </Grid>
                                </Border>

                                <Border Stroke="{StaticResource Border}" StrokeThickness="1" StrokeShape="RoundRectangle 12" Padding="16,14">
                                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="4">
                                        <RadioButton Grid.Column="0" GroupName="CheckInMode" IsChecked="{Binding IsGpsModeSelected}" InputTransparent="True" VerticalOptions="Center" />
                                        <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                                            <Label Text="GPS" Style="{StaticResource TimeOnSectionTitle}" FontAttributes="None" Margin="0" />
                                            <Label Text="Medewerker klokt in via knop wanneer hij op de gekozen werklocatie is." Style="{StaticResource TimeOnSubtitle}" />
                                        </VerticalStackLayout>
                                        <Grid.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding SelectGpsModeCommand}" />
                                        </Grid.GestureRecognizers>
                                    </Grid>
                                </Border>
                            </VerticalStackLayout>

                            <Label Text="{Binding CheckInModeError}" IsVisible="{Binding HasCheckInModeError}" Style="{StaticResource TimeOnError}" />

                            <VerticalStackLayout IsVisible="{Binding ShowQrSection}" Spacing="12" Margin="0,8,0,0">
                                <Label Text="QR-code voor inklokken" Style="{StaticResource TimeOnSectionTitle}" Margin="0" />

                                <Label Text="Laat medewerkers deze QR-code scannen of de koppelcode invoeren." Style="{StaticResource TimeOnSubtitle}" />

                                <Border Stroke="{StaticResource Border}" StrokeThickness="1" StrokeShape="RoundRectangle 12" Padding="16" HorizontalOptions="Center">
                                    <Image Source="{Binding QrCodeImage}" HeightRequest="220" WidthRequest="220" Aspect="AspectFit" />
                                </Border>

                                <Label Text="{Binding PairingCode}" Style="{StaticResource TimeOnTitle}" HorizontalTextAlignment="Center" CharacterSpacing="8" />

                                <Label Text="{Binding QrValidUntilText}" Style="{StaticResource TimeOnSubtitle}" HorizontalTextAlignment="Center" />
                            </VerticalStackLayout>

                            <VerticalStackLayout x:Name="GpsLocationSection" IsVisible="{Binding ShowGpsSection}" Spacing="12" Margin="0,8,0,0">
                                <Label Text="Bedrijfslocatie" Style="{StaticResource TimeOnSectionTitle}" Margin="0" />

                                <Label Text="Kies de werklocatie waarbinnen medewerkers mogen inklokken." Style="{StaticResource TimeOnSubtitle}" />

                                <VerticalStackLayout BindableLayout.ItemsSource="{Binding WorkLocations}" Spacing="8">
                                    <BindableLayout.ItemTemplate>
                                        <DataTemplate x:DataType="vm:WorkLocationItemViewModel">
                                            <Border Stroke="{StaticResource Border}" StrokeThickness="1" StrokeShape="RoundRectangle 12" Padding="16,14">
                                                <HorizontalStackLayout Spacing="12" VerticalOptions="Center">
                                                    <RadioButton IsChecked="{Binding IsSelected}" GroupName="WorkLocation" InputTransparent="True" VerticalOptions="Center" MinimumHeightRequest="24" MinimumWidthRequest="24" />
                                                    <Label Text="{Binding Name}" Style="{StaticResource TimeOnSectionTitle}" FontAttributes="None" Margin="0" VerticalOptions="Center" VerticalTextAlignment="Center" />
                                                    <HorizontalStackLayout.GestureRecognizers>
                                                        <TapGestureRecognizer Command="{Binding Source={x:Reference GpsLocationSection}, Path=BindingContext.SelectWorkLocationCommand}" CommandParameter="{Binding .}" />
                                                    </HorizontalStackLayout.GestureRecognizers>
                                                </HorizontalStackLayout>
                                            </Border>
                                        </DataTemplate>
                                    </BindableLayout.ItemTemplate>
                                </VerticalStackLayout>
                            </VerticalStackLayout>
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Border>
            </VerticalStackLayout>
        </ScrollView>
    </Grid>

</pages:MvvmContentPage>
